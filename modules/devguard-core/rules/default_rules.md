# Default Rules (v0.1)

- `DG001` high: SQL injection pattern via concatenated queries
- `DG002` high: unsafe deserialization primitives
- `DG003` high: hardcoded secret material
- `DG004` medium: expensive allocations in loops
- `DG005` medium: async/network calls missing local error handling
